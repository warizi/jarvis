/** @jsxImportSource @emotion/react */

import { taskViewStyles } from "./TaskView.style";

function TaskView() {
  const { container } = taskViewStyles;
  return <div css={container}> task</div>;
}

export default TaskView;
